<Window x:Class="Prakt15.ManageTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление тегами"
        Height="450"
        Width="600"
        Background="#dfeaeb"
        WindowStartupLocation="CenteArScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="#00C4B4" 
                Padding="15"
                CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Управление тегами" 
                           FontSize="20" 
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Теги используются для пометки товаров"
                           FontSize="12"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" 
                Background="White" 
                Margin="0,10,0,0"
                Padding="10"
                CornerRadius="5">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <Label Content="Поиск тега:" 
                           FontWeight="Bold"
                           Foreground="#333333"/>
                    <TextBox x:Name="txtSearch" 
                             Height="35"
                             FontSize="14"
                             Padding="5"
                             TextChanged="TxtSearch_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderBrush" Value="#00C4B4"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>

                <ListView Grid.Row="1" 
                          x:Name="lstTags"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8"
                                    Margin="0,0,0,5"
                                    Background="#f8f8f8"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="#"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="#00C4B4"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Name}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="#333333"
                                               VerticalAlignment="Center"
                                               Margin="0,0,15,0"
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Products.Count, StringFormat='Товаров: {0}'}"
                                               FontSize="12"
                                               Foreground="#666666"
                                               VerticalAlignment="Center"
                                               Margin="0,0,15,0"
                                               MinWidth="70"/>

                                    <StackPanel Grid.Column="3" 
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Button Content="Изменить"
                                                Background="#00C4B4"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                Width="80"
                                                Height="30"
                                                FontSize="12"
                                                Padding="5"
                                                Tag="{Binding Id}"
                                                Click="BtnEdit_Click"
                                                Margin="0,0,8,0"
                                                Cursor="Hand"/>

                                        <Button Content="Удалить"
                                                Background="#00C4B4"
                                                Foreground="White"
                                                FontWeight="Bold"
                                                Width="80"
                                                Height="30"
                                                FontSize="12"
                                                Padding="5"
                                                Tag="{Binding Id}"
                                                Click="BtnDelete_Click"
                                                Cursor="Hand"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Row="2" 
                Background="White" 
                Margin="0,10,0,0"
                Padding="15"
                CornerRadius="5">

            <StackPanel>
                <Label Content="Добавить новый тег:" 
                       FontWeight="Bold"
                       Foreground="#333333"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="#"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#00C4B4"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>

                    <TextBox x:Name="txtNewTag" 
                             Grid.Column="1"
                             Height="40"
                             FontSize="16"
                             Padding="10"
                             VerticalContentAlignment="Center"
                             MinWidth="250">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderBrush" Value="#00C4B4"/>
                                <Setter Property="BorderThickness" Value="2"/>
                                <Style.Triggers>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#00b4c4"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <Button Grid.Column="2" 
                            Content="ДОБАВИТЬ"
                            Background="#00b4c4"
                            Foreground="White"
                            FontWeight="Bold"
                            Width="120"
                            Height="40"
                            FontSize="14"
                            Margin="15,0,0,0"
                            Click="BtnAdd_Click"
                            Cursor="Hand"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>